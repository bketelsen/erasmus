version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - bodyclose
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unconvert
    - unused
    - whitespace

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofmt
